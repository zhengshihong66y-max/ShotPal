# MiMo 下一步任务要求：进度条与素材库卡片

日期：2026-05-24

本文是给小米 MiMo 或其他 AI 接手下一轮开发的执行文档。当前只做两个任务：

1. 优化预览区的进度条系统，把音频波形升级成真正可交互的进度条。
2. 优化素材库卡片，让视频封面和标题信息成为一个整体。

除此之外，暂时不要做自动场景切分、剪辑点菜单、标签持久化、全屏播放器、数据库重构或大范围架构拆分。

## 项目上下文

项目路径：

```text
/Users/zhengshihong/Downloads/Newtybei知识库/进行项目/拉片宝/LapianBao
```

核心文件：

- `LapianBao/ContentView.swift`
- `LapianBao/LibraryStore.swift`
- `LapianBao/LapianBaoApp.swift`
- `README.md`

当前 App 是一个 macOS SwiftUI 本地视频素材库。整体视觉参考 Apple Music for macOS，采用暗色、半透明、液态玻璃、较克制的工具界面风格。

## 总体原则

- 只做本文定义的两个任务。
- 不要引入新依赖。
- 不要重新使用 SwiftUI 原生 `VideoPlayer` 作为主播放器。
- 不要破坏 `Design` 中已有的圆角、间距和比例关系。
- 不要把界面改成网页后台、表格管理器或营销页。
- 每次修改后必须编译验证。

编译命令：

```bash
xcodebuild -project LapianBao.xcodeproj -scheme LapianBao -destination 'platform=macOS' build
```

## 任务一：优化进度条系统

### 当前问题

目前预览区有两个和播放进度相关的东西：

- 自定义播放器控制条里的细进度条。
- 视频下方的音频波形。

这会让信息重复。用户希望直接把声波图当成进度条系统，也就是波形不只是装饰，而是主要播放进度控制。

### 本轮目标

把 `AudioWaveformView` 升级为主进度条：

- 波形显示音频强弱。
- 波形上显示已经播放的部分。
- 波形上显示当前播放头。
- 用户点击波形任意位置可以跳转。
- 用户拖动波形可以连续 seek。
- 波形承担主要进度控制职责。
- 原播放器控制条里的细进度条应弱化或移除，避免两个进度条互相抢。

### 视觉要求

波形应该位于视频画面下方，成为预览区第二重要的元素。

建议结构：

```text
预览标题行
视频画面
波形进度条
当前视频信息与标签区
```

波形样式要求：

- 高度控制在 `56` 到 `76` 之间。
- 使用半透明玻璃底，不要用厚重实色背景。
- 未播放部分使用低透明度白色。
- 已播放部分使用更高透明度白色，或轻微强调色。
- 播放头使用一条细竖线或小圆点，不要太粗。
- 点击和拖动时要有即时反馈。
- 整体圆角使用 `Design.innerRadius`。

### 交互要求

必须实现：

- 点击波形跳转到对应时间。
- 拖动波形时更新播放位置。
- 播放时波形进度持续更新。
- 切换视频后波形进度归零，并加载新视频波形。

可以先不做：

- hover 时间气泡。
- 缩放时间线。
- 多轨波形。
- 入点/出点。
- 自动场景切分。
- 剪辑点竖线。

### 代码方向

当前相关结构在 `ContentView.swift`：

- `AudioWaveformView`
- `PreviewPlayerView`
- `PreviewScrubber`
- `previewPanel`
- `seekPreview(to:)`
- `updatePreviewProgress()`

建议做法：

1. 让 `AudioWaveformView` 接收更多参数：

```swift
let samples: [Double]?
let progress: Double
let seek: (Double) -> Void
```

2. 在 `AudioWaveformView` 内部增加点击和拖动手势。
3. 在绘制波形时，根据 `progress` 区分已播放和未播放部分。
4. 增加播放头绘制。
5. 将 `previewPanel` 中的 `AudioWaveformView` 接到 `previewProgress` 和 `seekPreview`。
6. 评估 `PreviewScrubber` 是否保留。如果保留，只作为控制条里的极简辅助，不再是主要进度条。

### 验收标准

- 波形可以点击跳转。
- 波形可以拖动跳转。
- 播放时波形上的进度会同步推进。
- 自定义播放器不会出现系统原生黑色悬停遮罩。
- 视觉上只有一个主要进度系统，用户不会困惑。
- 编译通过。

## 任务二：优化素材库卡片

### 当前问题

目前素材库卡片像是“一个卡片里又放了一个封面框”，封面和文字是分离的。用户希望封面不要被框在小窗口里，而是直接成为整张卡片的主体，标题和标签通过渐变自然叠在封面上。

### 本轮目标

重做素材库中的视频卡片：

- 视频封面 full-bleed 铺满整张卡片。
- 不再把封面单独框在一个小圆角窗口里。
- 标题、时长、标签与封面整合。
- 底部使用渐变或半透明遮罩承载文字。
- 卡片整体仍然保持统一圆角。

### 视觉要求

推荐卡片结构：

```text
整张卡片 = 视频封面
底部渐变区域 = 标题 + 标签
右上或右下 = 时长胶囊
选中状态 = 轻边框或轻微材质增强
```

具体要求：

- 封面使用 `.scaledToFill()` 铺满卡片。
- 卡片比例保持 16:9 或接近 16:10，先不要做竖卡。
- 底部渐变从透明过渡到黑色半透明。
- 标题放在渐变区域内，最多 1 到 2 行。
- 标签放在标题下方，最多显示一行。
- 时长用小胶囊显示，不能抢标题。
- 选中状态要明显，但不要使用刺眼颜色。
- 外层圆角使用 `Design.innerRadius` 或当前卡片已有标准。

### 交互要求

必须保持：

- 点击卡片后选中视频。
- 选中视频后右侧预览自动播放。
- 标签仍然可以显示。
- 网格大小滑条仍然正常工作。

可以先不做：

- 卡片右键菜单。
- 重新生成封面按钮。
- 批量选择。
- 拖拽排序。
- 多选标签编辑。

### 代码方向

当前相关结构在 `ContentView.swift`：

- `videoGrid(isCompact:)`
- `videoTile(_:)`
- `thumbnailImage(for:)`
- `durationTextIfReady(for:)`
- `tagChips(for:)`

建议做法：

1. 重写 `videoTile(_:)` 的内部层级。
2. 让封面作为卡片底层背景。
3. 在底部叠加渐变。
4. 把标题、标签、时长放入覆盖层。
5. 移除“封面框 + 下方文字块”的分离结构。
6. 保留选中态 overlay，但降低视觉噪声。

### 验收标准

- 素材库卡片看起来是一个整体，而不是卡片里套封面框。
- 封面占据主要视觉面积。
- 标题和标签能读清楚。
- 时长标记清楚但不突兀。
- 网格三档大小下都不挤、不重叠。
- 点击卡片仍能自动播放右侧预览。
- 编译通过。

## 标题系统补充要求

本轮只需要顺手规范进度条和素材卡片相关标题，不要重做全局标题系统。

要求：

- 面板标题 `素材库`、`预览` 继续使用同一风格和同一基线。
- 素材卡片标题使用 `.callout.weight(.semibold)` 或相近级别。
- 预览区当前视频名使用 `.title3.weight(.semibold)`。
- 时长和标签使用 `.caption` 或 `.caption2`。
- 不要加入说明性长文案。

## 明确不做的内容

本轮不要做这些：

- 自动场景切分。
- 达芬奇式剪辑点识别。
- 剪辑点菜单。
- 标记菜单。
- 信息菜单。
- 标签持久化。
- 数据库。
- 全屏播放器。
- 外部依赖。
- 大规模文件拆分。
- 重新设计窗口外框和红黄绿按钮。

如果用户之后确认做自动场景切分，再另写一份单独需求文档。

## 给 MiMo 的执行提示

```text
你现在只做两个任务：

1. 把预览区的视频下方音频波形升级成主进度条，支持显示播放进度、播放头、点击跳转、拖动跳转。
2. 重做素材库视频卡片，让封面 full-bleed 铺满卡片，标题、标签、时长通过底部渐变叠在封面上。

不要做自动场景切分，不要做剪辑点菜单，不要做标签持久化，不要做全屏播放器，不要引入外部依赖。

修改前先阅读 README.md、LapianBao/ContentView.swift、LapianBao/LibraryStore.swift。
修改后运行：
xcodebuild -project LapianBao.xcodeproj -scheme LapianBao -destination 'platform=macOS' build

如果无法看到运行画面，请明确说明只完成代码与编译验证。
```
