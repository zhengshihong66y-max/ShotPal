# 拉片宝下一阶段升级需求

日期：2026-05-25

这份文档整理下一阶段需要讨论和实现的产品升级方向。核心目标是：让拉片宝更适合快速看片、选帧、批注、截图、导出和复习，而不是变成传统剪辑软件。

## 1. 总体目标

拉片宝的设计理念是帮助用户以最快速度分析一部影片，并把值得学习的画面、时间点、批注和色彩信息沉淀成可以复用的学习材料。

下一阶段升级的关键词：

- 更灵活的工作区布局。
- 更紧凑、更适合拉片的时间线。
- 更快的截图、导出和选帧流程。
- 场景网格同时承担“场景切分展示板”和“已采样画面查看板”。
- 批注、截图、导出都要能回到原始视频时间点。
- 画面、声音、内容三个独立工作区分别承载深度分析、声音采样和原脚本转写。

本轮已经收束的主要模块：

```text
侧边栏：Finder 式主工作区入口，可收起但保留窄 rail
主页：看片、时间线、场景识别、截图、批注、声音采样
画面：已收集画面的网格、预览、色彩分析、回到原视频
声音：In / Out 选择、导出声音片段、声音片段列表
内容：章节时间线、原脚本、歌词式滚动、本地 Whisper 转写
```

## 2. 侧边栏与主界面布局

当前主界面应保持这个顺序：

```text
左侧标签栏 / 侧边栏 | 素材区 | 主编辑区
```

### 2.1 侧边栏定位

侧边栏不再只是标签筛选区，而是主工作区入口。它的内容和窗口栏已经确定，后续不要随意改成网页式导航。

侧边栏主要入口：

```text
主页
画面
声音
内容
```

定义：

- 主页：当前主工作台，负责素材预览、场景识别、时间线、截图、批注和快速采样。
- 画面：已收集画面的分析工作台，用于查看、管理和分析截图 / 导出画面。
- 声音：用于管理通过 In / Out 导出的声音片段。
- 内容：用于管理转写得到的原脚本、章节时间线和后续 Markdown 导出。

这些入口是页面 / 工作区切换，不是标签筛选。

### 2.2 Finder 风格要求

侧边栏需要保留当前接近 Apple 原生软件的视觉风格。后续修改时不要破坏这些关键设计：

- 左上角红黄绿窗口按钮的位置。
- 侧边栏顶部窗口区域。
- 侧边栏的大圆角矩形壳体。
- Finder 式暗色质感。
- 分组和条目的 macOS 原生排布感。
- 选中项的圆角矩形背景。

要学习 Finder 的是结构和排版风格，不是具体内容。

需要参考：

- 窗口按钮区域。
- 侧边栏宽度和留白。
- 条目行高。
- 图标和文字间距。
- 分组标题样式。
- 选中态圆角矩形。

不需要照搬：

- Finder 的红色、橙色、黄色等标签色。
- iCloud、隔空投送、位置等 Finder 特有内容。
- Finder 的标签颜色体系。

### 2.3 侧边栏收起逻辑

侧边栏一定要支持收起，以便给素材区和主编辑区释放空间。

但侧边栏收起时不应该完全消失。建议保留一条窄 rail，而不是让素材库直接贴到窗口最左侧。

原因：

- 可以保留红黄绿窗口按钮的视觉归属。
- 可以保留当前满意的圆角矩形和原生窗口壳体。
- 可以保留主页 / 画面 / 声音 / 内容的图标入口。
- 素材库仍然会左移并释放空间，但不会破坏整体 macOS 原生感。

布局规则：

```text
展开状态：
[侧边栏 272px] [素材区] [主编辑区]

收起状态：
[窄侧边栏 56px] [素材区] [主编辑区]
```

建议宽度：

```text
展开：260px - 280px，优先约 272px
收起：52px - 56px
```

收起后：

- 窗口按钮仍在左上角原位置。
- 隐藏文字和分组标题。
- 只保留工作区图标。
- 选中态仍然保留圆角背景。
- 素材区移动到窄 rail 右侧。
- 主编辑区自动获得更多空间。

不建议第一版做完全 0 宽隐藏。未来如果需要极限专注模式，可以再考虑第二层“完全隐藏侧边栏”。

### 2.4 左侧标签栏

如果后续仍保留标签，标签不应该成为侧边栏的主体。标签是筛选条件，不是主工作区入口。

要求：

- 标签优先放在当前页面或素材区顶部，作为筛选器。
- 如需放入侧边栏，也只能作为辅助分组，不能和“主页 / 画面 / 声音 / 内容”混成同一层级。
- 标签不需要自由调宽。

### 2.5 素材区

素材区需要灵活调整宽度。

要求：

- 支持拖拽调宽。
- 默认展开，不建议默认收起。
- 可以通过明确按钮收起。
- 收起后可以保留窄条或入口。
- 用户调过的宽度应保存，下次打开沿用。

建议宽度：

```text
最小：260px
默认：320px - 360px
最大：520px 或主窗口宽度的合理比例
```

### 2.6 素材库排序

素材库需要新增排序方式，设计参考 Finder 的排序控件和图标风格。

Finder 常见排序维度包括：

```text
名称
种类
日期添加
日期修改
日期创建
大小
标签
```

拉片宝不需要直接照搬所有文件管理维度，应转译成视频素材库有用的排序：

```text
名称
导入时间
修改时间
片长
文件大小
帧率
分辨率
标签
```

第一版建议保留：

- 名称。
- 导入时间。
- 片长。
- 文件大小。
- 分辨率。
- 标签。

排序方向：

- 升序。
- 降序。

界面设计：

- 在素材区顶部工具栏加入排序按钮。
- 图标参考 Finder 的排序 / 排列按钮风格，优先使用 SF Symbols。
- 按钮点击后弹出 macOS 风格菜单。
- 当前排序项需要在菜单中显示勾选状态。
- 排序方向用上箭头 / 下箭头或菜单项表达。
- 不要使用网页式下拉框。

建议图标：

```text
排序菜单：arrow.up.arrow.down 或 line.3.horizontal.decrease.circle
升序：arrow.up
降序：arrow.down
```

菜单示意：

```text
排序方式
  名称
  导入时间
  片长
  文件大小
  分辨率
  标签

方向
  升序
  降序
```

排序设置需要保存，下次打开素材库时沿用。

### 2.7 主编辑区

主编辑区是核心工作区。

要求：

- 自动吃掉剩余空间。
- 不写死宽度。
- 随着侧边栏收起、素材区缩窄而自然变大。
- 设置最小安全宽度，避免内容被压坏。

## 3. 素材卡片交互优化

当前素材卡片上的三个点按钮比较难点击，需要优化。

要求：

- 三点图标视觉上可以小，但点击热区必须更大。
- 建议热区至少为 32px x 32px 或 36px x 36px。
- 点击三点按钮时不能触发卡片本体的选择、打开或拖拽逻辑。
- hover 时要有明确反馈。
- 菜单层级不能被卡片、滚动区或预览区遮挡。

交互规则：

```text
点击卡片主体：选中 / 打开素材
点击三点按钮：打开更多菜单
```

## 4. 时间线重设计

当前时间线的问题是：时间信息单独占了一大块空间，导致视频和主内容区域无法充分显示。下一阶段时间线应更紧凑，把空间还给画面和场景分析。

### 4.1 时间线的基本结构

建议改为两层：

```text
第一层：完整宽度时间线
第二层：时间码 + 控制按钮
```

示意：

```text
[ 场景比例时间线 / 播放头 / 截图点 / 批注点 ]

[上一场景] [后退] [播放/暂停] [前进] [下一场景]   00:12:34:08   [截图] [批注]
```

### 4.2 时间码位置

时间码不要再单独占据很大区域。

可选方案：

- 固定放在控制栏居中位置。
- 跟随播放头显示在时间线下方。

第一版建议：

- 使用固定居中时间码。
- 这样更稳定，不需要处理边缘避让、抖动和遮挡问题。

### 4.3 核心控制

控制栏只放最重要的操作。

必须有：

- 播放 / 暂停。
- 截图。
- 批注。

建议有：

- 后退。
- 前进。
- 上一个场景。
- 下一个场景。

不建议堆太多剪辑软件式按钮。

### 4.4 时间线缩放

时间线的总体容器长度不变，始终占满当前可用宽度。缩放改变的是时间刻度密度，而不是把时间线本体拉长。

规则：

- 最小缩放：完整显示全片。
- 不能继续缩小到比全片更小。
- 放大时，同样宽度内显示更短时间范围和更多细节。
- 支持双指缩放。
- 支持拖动平移当前可见时间窗口。
- 缩放需要丝滑、连续，而不是几个生硬档位。

关键交互：

- 双指缩放时以手势中心或鼠标位置为锚点。
- 缩放前指针下方是什么时间点，缩放后仍尽量保持同一时间点。
- 播放头、截图点、批注点都要随缩放正确定位。
- 缩到全片边界时可以有轻微阻尼，但最终不能超过边界。

### 4.5 刻度显示

刻度应随缩放自动调整粒度。

示例：

```text
全片视图：5min / 10min 刻度
中等缩放：30s / 1min 刻度
细节缩放：1s / 5s 刻度
更细缩放：帧级或半秒级刻度
```

## 5. 批注功能

时间线需要支持“批注”。

### 5.1 添加批注

流程：

```text
移动播放头到某个时间点
点击“批注”
时间线上出现批注点
弹出文本框
输入内容并保存
```

也可以支持在时间线上右键或长按某点添加批注。

### 5.2 批注点交互

要求：

- 批注点显示在时间线上。
- 点击批注点时，视频跳到对应时间点。
- 点击后可以显示批注文本。
- 可以编辑和删除批注。
- 批注应该保存对应视频、时间码和文本内容。

建议数据：

```text
批注 ID
所属视频
时间点
批注文本
创建时间
更新时间
```

### 5.3 批注和截图的关系

批注和截图都属于时间点采样：

```text
截图：保存画面
批注：保存文字观察
```

两者都应该能在时间线上留下标记，并能一键回到原视频时间点。

## 6. 场景识别入口与进度

在还没有识别场景时，下方空白区域应该直接承担“识别场景”的入口。

### 6.1 空状态

未识别场景时：

```text
视频预览区

下方空白区域：
[识别场景]
```

要求：

- 识别按钮直接放在下方空白处。
- 不要让空白区域浪费。
- 按钮应是当前状态下最明确的下一步动作。

### 6.2 识别中

点击识别后：

```text
正在识别场景...
[真实进度条 42%]
```

要求：

- 显示真实进度条，而不是纯假 loading。
- 如果底层只能提供阶段进度，至少显示当前阶段。
- 识别期间按钮禁用或变成处理中状态。
- 失败时显示重试入口。

可能的阶段：

```text
读取视频
抽取关键帧
计算镜头差异
生成场景切分
创建缩略图
```

### 6.3 识别完成

识别完成后：

```text
视频预览区

场景网格
```

要求：

- 原来的空状态和识别按钮消失。
- 下方区域直接展示场景网格。
- 场景网格成为后续浏览、截图、导出和分析的主区域。

## 7. 场景网格、截图与导出

场景网格不仅是场景切分结果，也应该成为导出和采样状态的展示板。

### 7.1 网格的双重身份

场景网格同时承担：

```text
场景切分展示板
已截图 / 已导出画面查看板
```

用户看网格时，不仅能知道影片结构，也能知道自己已经采样、导出了哪些画面。

### 7.2 网格标注方式

场景网格卡片不应该主要标注“时间点”。用户更关心的是：从这个网格开始的这个场景持续了多久。

标注规则：

```text
不足 1 秒：显示帧数，例如 18 帧
1 秒以上：显示秒数 + 帧数，例如 3 秒 12 帧
```

要求：

- 每个场景代表帧显示该场景的持续时长。
- 持续时长从当前场景起点计算到下一个场景起点。
- 最后一个场景从自身起点计算到视频结束。
- 显示文案应尽量短，不要挤占画面。
- 具体时间码可以作为 hover 信息、详情信息或辅助信息，不作为网格卡片的主要标注。

示例：

```text
18 帧
2 秒 06 帧
12 秒 18 帧
```

帧数计算需要基于视频真实帧率，例如 24fps、25fps、30fps。不要简单假设固定帧率。

### 7.3 导出已有网格帧

当用户点击某个网格帧，再按“导出”：

- 如果这张图已经存在于网格中，就直接导出该帧。
- 导出完成后，这个网格帧变成已导出状态。
- 已导出状态可以用橙色边框表示。

规则：

```text
点击已有网格帧 + 导出 = 导出这个已有帧，并标记为已导出
```

### 7.4 截取当前播放帧

如果用户想导出的不是网格已有的首帧或代表帧，而是当前播放位置的某一帧：

- 点击“截图”按钮或使用快捷键。
- 从当前播放时间截取一帧。
- 这张新图片直接按时间顺序插入场景网格。
- 新插入图片自动视为已导出 / 已采样。
- 新插入图片显示橙色边框。

规则：

```text
截图 = 从当前视频时间生成新帧 + 插入网格 + 视为已导出
```

### 7.5 截图和导出的关系

截图和导出不是完全同一个逻辑，但有重叠。

区别：

```text
导出：针对已经存在于网格中的帧，把它输出并标记状态
截图：针对当前播放位置生成新帧，插入网格，并立即视为已导出
```

也就是说：

- 截图本身可以被视为一种立即导出。
- 导出可以作用于已有网格帧。

### 7.6 网格状态视觉

建议状态：

```text
普通场景代表帧：默认边框
已导出代表帧：橙色边框
用户手动截图帧：橙色边框 + 小相机标记
当前选中帧：额外选中态
```

橙色边框的含义：

```text
这张画面已经进入采样 / 导出集合
```

### 7.7 插入顺序

用户截图的新帧应按时间码插入网格，而不是简单追加到最后。

这样网格仍然保持影片时间顺序。

### 7.8 点击网格帧

点击任意网格帧：

- 视频跳转到该帧对应时间点。
- 该网格帧变成当前选中。
- 用户可以继续导出、批注、分析或回看。

## 8. 提取画面界面与信息显示

提取画面界面需要保持轻量，不做复杂资料面板。它的目标是帮助用户快速判断当前视频的基础规格，以及每个网格画面对应的镜头持续时间。

### 8.1 是否显示名字

当前视频的名字可以显示，但不应该在每张网格卡片上重复显示。

建议：

- 在提取画面界面的标题区显示当前视频名。
- 网格卡片内不重复显示完整视频名。

原因：

- 当前场景网格通常属于一个正在分析的视频，重复显示名字会浪费空间。
- 视频名适合放在区域标题中。

### 8.2 网格卡片上优先显示的信息

网格卡片上最有用的信息应尽量少而准。

优先级：

```text
1. 该场景持续时长：几秒几帧，或不足一秒时显示几帧
2. 已导出 / 已采样状态
3. 手动截图标记
4. 当前选中状态
```

不建议默认显示：

- 完整视频名。
- 完整路径。
- 过长时间码。
- 大段说明文字。

### 8.3 当前视频基础信息

提取画面界面只需要显示非常克制的视频基础信息。

保留：

- 帧率，例如 `24 帧`、`25 帧`、`30 帧`。
- 分辨率，例如 `1920x1080`。

建议显示形式：

```text
花样年华.mov    24 帧 · 1920x1080
```

不规划复杂 hover 信息区，也不规划大段详情面板。

### 8.4 帧率与时长计算

帧率对拉片很有用，尤其是判断一个镜头到底是二十几帧，还是一秒以上。

要求：

- App 需要读取视频真实帧率。
- 界面显示优先使用中文形式，例如 `24 帧`、`25 帧`。
- 场景时长的“几秒几帧”应基于真实帧率计算。

建议显示位置：

```text
当前视频信息区：24 帧 · 1920x1080
网格卡片：2 秒 06 帧
```

### 8.5 网格预览大小调节

场景网格生成之后，需要像素材库一样提供缩略图大小调节能力。

要求：

- 在场景网格区域提供一个滑块或分段式大小控制。
- 用户可以调整网格预览图大小。
- 调整时网格重新排布，但保持时间顺序。
- 用户设置应保存，下次打开沿用。
- 这个控制只影响场景网格，不影响素材库卡片大小。

建议：

```text
小 / 中 / 大
```

或者复用素材库现有的三段式滑块设计，保持界面一致。

## 9. 画面页面

侧边栏中的“画面”不是主页预览区的重复，而是已收集画面的分析工作台。

主页负责快速看片、截图、导出和采样；画面页面负责对已经收集到的画面进行更仔细的查看和分析。

### 9.1 页面定位

画面页面的核心价值：

```text
主页：快速采样
画面：深度分析
```

画面页面用于：

- 管理已截图 / 已导出的画面。
- 放大查看单张画面。
- 分析画面颜色。
- 查看画面来源信息。
- 一键回到原视频对应时间点。

### 9.2 页面排布

画面页面建议延续素材库的操作逻辑：

```text
左侧：已收集画面网格
右侧：大图预览 + 画面分析信息
```

左侧画面网格显示：

- 导出的场景代表帧。
- 手动截图帧。
- 收藏或标星画面。
- 后续可能加入的对比帧。

右侧显示：

- 当前选中画面的大图预览。
- 色彩分析工具。
- 基础来源信息。
- 回到原视频按钮。

### 9.3 存放内容

每个被采样的画面应至少保存：

- 缩略图。
- 原视频。
- 时间码。
- 所属场景。
- 类型：场景代表帧 / 手动截图帧。
- 是否已导出。
- 备注。
- 标签。
- 色彩分析数据。

### 9.4 回到原视频

每个采样画面都必须能一键跳回原视频对应时间点。

这是拉片学习工作流的关键要求。

点击“回到原视频”后：

- 切回主页。
- 打开对应视频。
- 跳转到该画面对应时间点。
- 必要时暂停在这一帧。
- 高亮对应场景网格帧。

### 9.5 分析能力

选中画面后，后续希望支持色彩分析：

- 直方图。
- 色卡。
- 主色提取。
- 明暗分布。
- 饱和度。
- 色温倾向。
- 相邻镜头对比。
- RGB 三通道图。
- 饱和度图。

第一版可以先做：

- 大图预览。
- 主色色卡。
- 简单直方图。
- 来源视频、时间点、帧率、分辨率。
- 回到原视频。

后续再逐步加入更重的调色和分析工具。

## 10. 声音提取与声音页面

声音提取也应该遵循“快速采样”的逻辑。因为声音本身没有画面，波形应承担主要界面。

### 10.1 波形作为进度条

预览区的声音进度条应变成可交互波形：

- 波形显示声音强弱。
- 波形显示当前播放进度。
- 波形支持点击跳转。
- 波形支持拖动 seek。
- 波形上可以显示 In / Out 选择区间。

### 10.2 In / Out 选择

声音提取通过 In / Out 完成：

```text
播放到某处 -> 点 In
播放到另一处 -> 点 Out
点导出声音
```

要求：

- In 点和 Out 点显示在波形上。
- In 到 Out 之间的区间高亮。
- 第一版可以先用按钮设置 In / Out。
- 后续再考虑拖动边界微调。

### 10.3 控制按钮位置

In / Out 和导出声音按钮可以放在视频界面下方的同一控制区域，与截图、批注属于同一类采样操作。

建议分组：

```text
画面：截图 / 批注
声音：In / Out / 导出声音
```

或者在控制栏中并列呈现：

```text
[播放] [截图] [批注] [In] [Out] [导出声音]
```

### 10.4 导出后的陈列

导出声音片段后，片段应直接陈列在当前视频下方。

每个声音片段至少显示：

- 小波形或声音条。
- In / Out 时间。
- 片段时长。
- 播放按钮。
- 回到原视频按钮。

第一版可简化为：

```text
[播放] 00:12:10 - 00:12:18 · 8 秒 [回到原视频]
```

### 10.5 声音页面

侧边栏中的“声音”页面用于集中管理所有已导出的声音片段。

主页负责采样：

```text
选择 In / Out -> 导出声音 -> 当前视频下方出现片段
```

声音页面负责管理：

```text
所有声音片段
按来源视频筛选
播放片段
查看波形
备注
回到原视频
导出文件
```

第一版声音页面可以先做轻量列表，不需要复杂剪辑功能。

## 11. 内容页面与本地转写

侧边栏中的“内容”页面用于把视频声音转成可以阅读、跳转和导出的原脚本。

### 11.1 页面定位

内容页面第一版不做复杂 AI 深度分析。它的核心是：

```text
上方：章节时间线
下方：完整原脚本 / 转写文本
```

它负责：

- 声音转文字。
- 生成逐句时间戳。
- 显示章节时间线。
- 显示完整原脚本。
- 播放时脚本像歌词一样自动滚动。
- 点击句子跳转到视频对应时间点。
- 导出 Markdown，供后续丢给 Codex 或 AI API 深度分析。

### 11.2 分析前状态

在没有分析之前，内容页面显示普通时间线框架和分析入口：

```text
[普通章节时间线骨架]

[开始分析]
```

此时不显示自动章节。

### 11.3 分析后状态

点击分析后，流程为：

```text
提取音频
本地语音转文字
生成逐句时间戳
生成章节时间线
展示完整原脚本
```

分析后页面：

```text
[章节时间线]
00:00 开场
02:14 冲突
06:30 转折
10:42 结尾

[原脚本]
00:00:03  第一句话
00:00:08  第二句话
00:00:14  第三句话
```

交互：

- 视频播放时，脚本自动滚动到当前句。
- 当前句高亮。
- 点击某一句，视频跳转到该句时间点。
- 点击章节，视频跳转到章节起点，脚本滚动到该章节第一句。

### 11.4 AI 分析边界

内容页面第一版不内置复杂 AI 深度分析。

原因：

- 本地转写更适合生成“原脚本草稿”和时间索引。
- 深度文本分析可能需要 AI API，或导出 Markdown 后交给 Codex 分析。
- 页面不应过早变成复杂 AI 聊天界面。

第一版建议提供：

- 导出 Markdown。
- 保留未来接入 AI API 的接口可能性。
- 允许用户手动校正脚本和章节。

### 11.5 本地转写技术准备

已经准备了本地语音转文字能力：

- 引擎：`Tools/whisper.cpp`
- 命令行工具：`Tools/whisper.cpp/build/bin/whisper-cli`
- 模型：`Tools/whisper.cpp/models/ggml-large-v3-turbo-q5_0.bin`
- 包装脚本：`Tools/transcribe_with_whisper.sh`
- 说明文档：`Tools/WHISPER_TRANSCRIPTION_SETUP.md`

模型策略：

```text
large-v3-turbo-q5_0
多语言
约 547 MiB
适合先做本地内容提取 MVP
```

包装脚本会输出：

```text
.txt
.srt
.json
```

`.json` 后续可用于逐句时间戳、歌词式滚动和点击跳转。

当前注意点：

- GPU / Metal 路径首次测试时出现崩溃。
- 包装脚本已默认走稳定的 CPU + Accelerate 路径。
- 已用 `Tools/whisper.cpp/samples/jfk.wav` 验证输出成功。
- 后续可单独排查 Metal，或改用 WhisperKit 做 Swift / Core ML 原生集成。

## 12. 导出目标

导出不只是保存图片，而是把画面证据变成可复用的学习材料。

### 12.1 第一阶段导出

优先支持：

- PNG / JPG 图片。
- 文件名包含顺序和时间码。
- 自动生成索引文件。
- 声音片段文件。
- 原脚本 Markdown。

示例：

```text
MovieName/
  selected-frames/
    001_00-03-12-08.png
    002_00-04-20-15.png
    index.md
```

内容脚本导出示例：

```md
# 视频名

## 章节

- [00:00] 开场
- [02:14] 冲突
- [06:30] 转折

## 原脚本

[00:00:03] 第一句话……
[00:00:08] 第二句话……
```

### 12.2 后续导出方向

后续可以支持：

- Markdown 拉片笔记。
- PDF 图集。
- 色卡报告。
- 对比图。
- Obsidian / Notion 友好格式。
- Anki 复习材料。

## 13. 建议的第一版实现顺序

第一版建议先做这些最能改善体验的内容：

1. 调整主布局：Finder 式侧边栏可收起并保留窄 rail，素材区可拖拽调宽，主编辑区自适应。
2. 优化素材卡片三点按钮点击热区。
3. 素材库新增 Finder 风格排序菜单，并保存排序偏好。
4. 重做时间线为“紧凑时间线 + 控制栏”。
5. 增加截图和批注按钮。
6. 场景识别前显示空状态按钮，识别中显示真实进度，完成后展示场景网格。
7. 让场景网格支持已导出状态和橙色边框。
8. 让截图帧按时间顺序插入网格，并自动标记为已导出。
9. 点击网格帧可以跳回原视频时间点。
10. 网格卡片显示场景持续时长，而不是只显示时间点。
11. 提取画面界面显示当前视频名、帧率和分辨率。
12. 场景网格提供缩略图大小滑块，允许用户调节预览大小。
13. 做出画面页面的基础形态：画面网格、大图预览、基础色彩分析、回到原视频。
14. 做出声音提取基础流程：波形进度条、In / Out、导出声音、声音片段列表。
15. 做出内容页面基础流程：章节时间线、原脚本、歌词式滚动、点击句子跳转。
16. 接入本地 Whisper 包装脚本，生成 `.json` / `.srt` / `.txt`，供内容页面使用。

## 14. 仍需继续讨论的问题

这些点先记录下来，后续再细化：

- 时间码最终是固定居中，还是跟随播放头。
- 截图和导出的快捷键分别是什么。
- 素材库排序第一版是否需要“帧率”和“分辨率”，还是先只做名称、导入时间、片长、大小、标签。
- 橙色边框是否足够表达“已导出”，是否还需要图标或筛选。
- 手动截图帧插入网格后，是否要归属于最近的场景。
- 选帧 / 分析区域到底放在素材区 tab、主编辑区面板，还是独立视图。
- 色彩分析第一版做到什么程度。
- 导出后的图片和索引文件存储在项目目录、视频目录，还是用户指定目录。
- 可变帧率视频的“几秒几帧”应该如何展示得既准确又不复杂。
- 画面页面的色彩分析第一版具体保留哪些图：直方图、色卡、RGB 三通道、饱和度图的优先级。
- 声音片段导出格式：wav、m4a、mp3 中第一版选择哪个。
- 声音片段是否需要备注、标签、收藏状态。
- 内容页面的章节时间线由什么生成：按静音、按固定时间、按转写文本语义，还是先手动。
- 转写语言默认值：自动识别、中文优先，还是跟随用户设置。
- 本地转写不够准时，何时引入 API 转写或 AI API 深度分析。
- Whisper 模型是否继续使用 `large-v3-turbo-q5_0`，还是提供更准但更大的 `large-v3` 选项。
